<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:textColor="@color/black"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Hora del pedido -->
    <TextView
        android:id="@+id/horaCompraTextView"
        android:textColor="@color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hora del pedido"
        android:textStyle="bold"
        android:textSize="16sp" />

    <!-- Layout para contener los ítems y precios dinámicamente -->
    <LinearLayout
        android:id="@+id/itemsLayout"
        android:textColor="@color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="8dp" />

    <!-- Total de ítems -->
    <TextView
        android:id="@+id/totalItemsTextView"
        android:textColor="@color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total de ítems"
        android:textSize="14sp" />

    <!-- Total del precio -->
    <TextView
        android:id="@+id/totalPrecioTextView"
        android:textColor="@color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total del precio"
        android:textSize="14sp" />

    <!-- Botón para eliminar -->
    <Button
        android:id="@+id/eliminarButton"
        android:backgroundTint="@color/botons"
        android:textColor="@color/background"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Eliminar"
        android:layout_gravity="end"
        android:paddingTop="8dp" />

</LinearLayout>
